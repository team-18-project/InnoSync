import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF40BA21);
  static const Color primaryDark = Color(0xFF298217);

  // Background colors
  static const Color background = Color(0xFFFFFFFF);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color cardBackground = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Color(0xFF757575);
  static const Color textOnPrimary = Colors.white;

  // Status colors
  static const Color error = Colors.red;
  static const Color success = Color(0xFF40BA21);
  static const Color warning = Color(0xFFFFA726);
  static const Color info = Colors.blue;

  // Border and shadow colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color shadow = Color(0xFFE0E0E0);

  // Special purpose colors
  static const Color profilePickerBackground = Color(0xFFF0F8F0);
  static const Color tabBackground = Color(0xFF298217);
}
